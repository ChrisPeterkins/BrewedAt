// Placeholder for analytics - to be implemented later
export const logPageView = (page: string) => {
  // console.log('Page view:', page);
};

export const logEvent = (event: string, data?: any) => {
  // console.log('Event:', event, data);
};

export const trackEventView = (eventId: string) => {
  // console.log('Event view:', eventId);
};

export const trackPodcastView = (episodeId: string) => {
  // console.log('Podcast view:', episodeId);
};
